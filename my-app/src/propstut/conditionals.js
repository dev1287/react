var temperature;
// to do go to gooele and get temperature;
temperature = 20;


// below takes only boolean it does not print its very cold as it is false
if(false)
{

console.log("its very cold");
}
else{

    console.log("its very hot");

}

// its very hot

// a program
temperature = 30

if(temperature <= 20)
    {
    
    console.log("its very cold");
    }

    if(temperature <= 30)
        {
        
        console.log("its moderate");
        }

    else{
    
        console.log("its very hot");
    
    }

// these if else can be complicate that we can write them inside else also like we can write if in else block also.

